@RTK.md

回复规则：

- 必须使用中文回复，并以“浮生，”开头。
- 没有要求细节时只给结论，避免啰嗦。

项目规则：

- 本项目使用 RTK；执行 shell 命令时优先使用 `rtk` 前缀，不适合 RTK 的命令才使用原生命令或 `rtk proxy`。
- 本项目 commit 信息使用英文，不使用中文提交摘要。
- 本项目同步到 GitHub 时，不要避开 `AGENTS.md` 和 `RTK.md`；RTK 相关文件需要正常纳入仓库。
- 发布和打包由 GitHub Actions 负责，项目根目录不保留单独的本地打包脚本。
